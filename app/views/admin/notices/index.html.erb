<div class="container">
    <div class="level"> 
        <div class="level-left">
            <h1 class="title">
                Notices (<%= @pagy.count %>)
            </h1>
        </div>

        <div class="level-right">
            <%= link_to "New Notice", new_admin_notice_path, class: "button is-light level-item" %>
        </div>
    </div>

    <%= render Notices::TableComponent.new(notices: @notices) %>
</div>

<hr/>
<div class="container" data-controller='pagy'>
    <div class="is-flex is-justify-content-center">
        <%== @pagy.input_nav_js(:bulma) %>
    </div>
</div>
