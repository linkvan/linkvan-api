<%= render Shared::CardComponent.new(title: 'Location') do |card| %>
    <% card.with_button(title: 'Add', path: new_admin_facility_location_path(facility_id: facility), icon_class: "fa-plus-square") %>
    <% card.with_button(title: 'Edit', path: edit_admin_facility_location_path(facility_id: facility, id: facility.id)) %>

    <div class="columns">
        <div class="column">
            <p><span class="tag is-primary">Current</span></p>
            <p><%= facility.address %></p>
            <p><span class="tag"><%= facility.coordinates.map(&:to_f).join(", ") %></span></p>
        </div>

        <div class="column is-one-third">
            <%= render Locations::EmbedMapComponent.new(facility.lat, facility.long, height: "100%") %>
        </div>
    </div>
<% end %>
